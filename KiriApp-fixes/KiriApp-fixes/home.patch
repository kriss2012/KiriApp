--- kiri_v2/KiriApp-2/app/src/main/java/com/kiriplatform/app/ui/screens/HomeScreen.kt	2026-06-11 19:22:11.000000000 +0000
+++ kiri_v2_fixed/app/src/main/java/com/kiriplatform/app/ui/screens/HomeScreen.kt	2026-07-04 03:25:20.905776610 +0000
@@ -4,6 +4,7 @@
 import androidx.compose.foundation.background
 import androidx.compose.foundation.border
 import androidx.compose.foundation.clickable
+import androidx.compose.foundation.isSystemInDarkTheme
 import com.kiriplatform.app.utils.clickableDebounced
 import androidx.compose.foundation.layout.*
 import androidx.compose.foundation.lazy.LazyColumn
@@ -142,8 +143,11 @@
 ) {
     LazyColumn(
         modifier = Modifier
-            .fillMaxSize(),
-        contentPadding = PaddingValues(bottom = 100.dp)
+            .fillMaxSize()
+            .statusBarsPadding(),
+        // Extra bottom padding so the last item always clears the floating nav pill
+        // + system nav bar, whether the device uses gesture or 3-button navigation.
+        contentPadding = PaddingValues(bottom = 140.dp)
     ) {
         item { HomeTopBar(onNavigateToNotifications = onNavigateToNotifications) }
         item { GreetingSection(userName = user.fullName) }
@@ -175,13 +179,14 @@
 
 @Composable
 fun ResumeBuilderCard(onClick: () -> Unit) {
+    val dark = isSystemInDarkTheme()
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onClick() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintSky,
+        color = if (dark) NotionTintSkyDark else NotionTintSky,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Row(
@@ -189,24 +194,25 @@
             verticalAlignment = Alignment.CenterVertically
         ) {
             Column(modifier = Modifier.weight(1f)) {
-                Text("AI CAREER TOOLS", style = MaterialTheme.typography.labelSmall, color = NotionBrandPurple800.copy(alpha = 0.6f), letterSpacing = 1.sp)
-                Text("ATS Resume Builder", style = MaterialTheme.typography.titleMedium, color = NotionCharcoal, fontWeight = FontWeight.Bold)
-                Text("Generate target-role optimized resume with Google X-Y-Z formula.", style = MaterialTheme.typography.bodySmall, color = NotionCharcoal.copy(alpha = 0.7f))
+                Text("AI CAREER TOOLS", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandPurple800.copy(alpha = 0.6f), letterSpacing = 1.sp)
+                Text("ATS Resume Builder", style = MaterialTheme.typography.titleMedium, color = if (dark) NotionOnTintDark else NotionCharcoal, fontWeight = FontWeight.Bold)
+                Text("Generate target-role optimized resume with Google X-Y-Z formula.", style = MaterialTheme.typography.bodySmall, color = if (dark) NotionOnTintDarkMuted else NotionCharcoal.copy(alpha = 0.7f))
             }
-            KiriIconBadge(icon = "📄", backgroundColor = NotionCanvas)
+            KiriIconBadge(icon = "📄", backgroundColor = if (dark) NotionBrandNavyMid else NotionCanvas)
         }
     }
 }
 
 @Composable
 fun MicroCredentialsCard(onClick: () -> Unit) {
+    val dark = isSystemInDarkTheme()
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onClick() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintYellow,
+        color = if (dark) NotionTintYellowDark else NotionTintYellow,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Row(
@@ -214,24 +220,25 @@
             verticalAlignment = Alignment.CenterVertically
         ) {
             Column(modifier = Modifier.weight(1f)) {
-                Text("VERIFIABLE CREDENTIALS", style = MaterialTheme.typography.labelSmall, color = NotionBrandBrown.copy(alpha = 0.8f), letterSpacing = 1.sp)
-                Text("Badges & Certifications", style = MaterialTheme.typography.titleMedium, color = NotionCharcoal, fontWeight = FontWeight.Bold)
-                Text("Showcase skill badges, achievements, and roadmap milestones.", style = MaterialTheme.typography.bodySmall, color = NotionCharcoal.copy(alpha = 0.7f))
+                Text("VERIFIABLE CREDENTIALS", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandBrown.copy(alpha = 0.8f), letterSpacing = 1.sp)
+                Text("Badges & Certifications", style = MaterialTheme.typography.titleMedium, color = if (dark) NotionOnTintDark else NotionCharcoal, fontWeight = FontWeight.Bold)
+                Text("Showcase skill badges, achievements, and roadmap milestones.", style = MaterialTheme.typography.bodySmall, color = if (dark) NotionOnTintDarkMuted else NotionCharcoal.copy(alpha = 0.7f))
             }
-            KiriIconBadge(icon = "🥇", backgroundColor = NotionCanvas)
+            KiriIconBadge(icon = "🥇", backgroundColor = if (dark) NotionBrandNavyMid else NotionCanvas)
         }
     }
 }
 
 @Composable
 fun ProjectShowcaseCard(onClick: () -> Unit) {
+    val dark = isSystemInDarkTheme()
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onClick() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintMint,
+        color = if (dark) NotionTintMintDark else NotionTintMint,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Row(
@@ -239,24 +246,25 @@
             verticalAlignment = Alignment.CenterVertically
         ) {
             Column(modifier = Modifier.weight(1f)) {
-                Text("PEER CODE REVIEW", style = MaterialTheme.typography.labelSmall, color = NotionBrandTeal.copy(alpha = 0.8f), letterSpacing = 1.sp)
-                Text("Project Showcase", style = MaterialTheme.typography.titleMedium, color = NotionCharcoal, fontWeight = FontWeight.Bold)
-                Text("Submit repositories, get peer feedback, and feature your projects.", style = MaterialTheme.typography.bodySmall, color = NotionCharcoal.copy(alpha = 0.7f))
+                Text("PEER CODE REVIEW", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandTeal.copy(alpha = 0.8f), letterSpacing = 1.sp)
+                Text("Project Showcase", style = MaterialTheme.typography.titleMedium, color = if (dark) NotionOnTintDark else NotionCharcoal, fontWeight = FontWeight.Bold)
+                Text("Submit repositories, get peer feedback, and feature your projects.", style = MaterialTheme.typography.bodySmall, color = if (dark) NotionOnTintDarkMuted else NotionCharcoal.copy(alpha = 0.7f))
             }
-            KiriIconBadge(icon = "💻", backgroundColor = NotionCanvas)
+            KiriIconBadge(icon = "💻", backgroundColor = if (dark) NotionBrandNavyMid else NotionCanvas)
         }
     }
 }
 
 @Composable
 fun CampusAmbassadorCard(onClick: () -> Unit) {
+    val dark = isSystemInDarkTheme()
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onClick() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintLavender,
+        color = if (dark) NotionTintLavenderDark else NotionTintLavender,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Row(
@@ -264,24 +272,25 @@
             verticalAlignment = Alignment.CenterVertically
         ) {
             Column(modifier = Modifier.weight(1f)) {
-                Text("CAMPUS AMBASSADOR", style = MaterialTheme.typography.labelSmall, color = NotionBrandPurple.copy(alpha = 0.8f), letterSpacing = 1.sp)
-                Text("Leaderboard & Referrals", style = MaterialTheme.typography.titleMedium, color = NotionCharcoal, fontWeight = FontWeight.Bold)
-                Text("Invite friends, earn points, and climb the campus rank list.", style = MaterialTheme.typography.bodySmall, color = NotionCharcoal.copy(alpha = 0.7f))
+                Text("CAMPUS AMBASSADOR", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandPurple.copy(alpha = 0.8f), letterSpacing = 1.sp)
+                Text("Leaderboard & Referrals", style = MaterialTheme.typography.titleMedium, color = if (dark) NotionOnTintDark else NotionCharcoal, fontWeight = FontWeight.Bold)
+                Text("Invite friends, earn points, and climb the campus rank list.", style = MaterialTheme.typography.bodySmall, color = if (dark) NotionOnTintDarkMuted else NotionCharcoal.copy(alpha = 0.7f))
             }
-            KiriIconBadge(icon = "📣", backgroundColor = NotionCanvas)
+            KiriIconBadge(icon = "📣", backgroundColor = if (dark) NotionBrandNavyMid else NotionCanvas)
         }
     }
 }
 
 @Composable
 fun InterviewSandboxCard(onClick: () -> Unit) {
+    val dark = isSystemInDarkTheme()
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onClick() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintRose,
+        color = if (dark) NotionTintRoseDark else NotionTintRose,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Row(
@@ -289,11 +298,11 @@
             verticalAlignment = Alignment.CenterVertically
         ) {
             Column(modifier = Modifier.weight(1f)) {
-                Text("AI CAREER SANDBOX", style = MaterialTheme.typography.labelSmall, color = NotionBrandPinkDeep.copy(alpha = 0.8f), letterSpacing = 1.sp)
-                Text("Interview Practice Sandbox", style = MaterialTheme.typography.titleMedium, color = NotionCharcoal, fontWeight = FontWeight.Bold)
-                Text("Practice domain-specific interviews and get a confidence evaluation report.", style = MaterialTheme.typography.bodySmall, color = NotionCharcoal.copy(alpha = 0.7f))
+                Text("AI CAREER SANDBOX", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandPinkDeep.copy(alpha = 0.8f), letterSpacing = 1.sp)
+                Text("Interview Practice Sandbox", style = MaterialTheme.typography.titleMedium, color = if (dark) NotionOnTintDark else NotionCharcoal, fontWeight = FontWeight.Bold)
+                Text("Practice domain-specific interviews and get a confidence evaluation report.", style = MaterialTheme.typography.bodySmall, color = if (dark) NotionOnTintDarkMuted else NotionCharcoal.copy(alpha = 0.7f))
             }
-            KiriIconBadge(icon = "🎙️", backgroundColor = NotionCanvas)
+            KiriIconBadge(icon = "🎙️", backgroundColor = if (dark) NotionBrandNavyMid else NotionCanvas)
         }
     }
 }
@@ -301,14 +310,16 @@
 @Composable
 fun AalInternshipCard(onboarding: AalOnboardingDto, activities: List<AalActivityDto>, onClick: () -> Unit) {
     val completedCount = activities.count { it.status == ActivityStatus.VERIFIED }
-    
+    val dark = isSystemInDarkTheme()
+    val headingColor = if (dark) NotionOnTintDark else NotionBrandPurple800
+
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onClick() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintLavender,
+        color = if (dark) NotionTintLavenderDark else NotionTintLavender,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Column(modifier = Modifier.padding(20.dp)) {
@@ -318,8 +329,8 @@
                 verticalAlignment = Alignment.CenterVertically
             ) {
                 Column {
-                    Text("KIRI AI INTERNSHIP", style = MaterialTheme.typography.labelSmall, color = NotionBrandPurple800.copy(alpha = 0.5f), letterSpacing = 1.sp)
-                    Text("Intelligence Progress", style = MaterialTheme.typography.titleMedium, color = NotionBrandPurple800, fontWeight = FontWeight.Bold)
+                    Text("KIRI AI INTERNSHIP", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandPurple800.copy(alpha = 0.5f), letterSpacing = 1.sp)
+                    Text("Intelligence Progress", style = MaterialTheme.typography.titleMedium, color = headingColor, fontWeight = FontWeight.Bold)
                 }
                 Surface(
                     color = NotionPrimary.copy(alpha = 0.1f),
@@ -342,14 +353,14 @@
                 Text(
                     "$completedCount / 7",
                     style = MaterialTheme.typography.titleLarge,
-                    color = NotionBrandPurple800,
+                    color = headingColor,
                     fontWeight = FontWeight.Bold
                 )
                 LinearProgressIndicator(
                     progress = { completedCount / 7f },
                     modifier = Modifier.weight(1f).height(8.dp).clip(CircleShape),
                     color = NotionPrimary,
-                    trackColor = NotionBrandPurple300.copy(alpha = 0.5f),
+                    trackColor = NotionBrandPurple300.copy(alpha = if (dark) 0.25f else 0.5f),
                     strokeCap = androidx.compose.ui.graphics.StrokeCap.Round
                 )
             }
@@ -358,7 +369,7 @@
             Text(
                 "Continue your journey to unlock Kiri Certification.",
                 style = MaterialTheme.typography.bodySmall,
-                color = NotionBrandPurple800.copy(alpha = 0.6f)
+                color = if (dark) NotionOnTintDarkMuted else NotionBrandPurple800.copy(alpha = 0.6f)
             )
         }
     }
@@ -366,13 +377,14 @@
 
 @Composable
 fun InnovationHubCard(onNavigateToHub: () -> Unit) {
+    val dark = isSystemInDarkTheme()
     Surface(
         modifier = Modifier
             .padding(horizontal = 24.dp, vertical = 8.dp)
             .fillMaxWidth()
             .clickableDebounced { onNavigateToHub() },
         shape = RoundedCornerShape(8.dp),
-        color = NotionTintPeach,
+        color = if (dark) NotionTintPeachDark else NotionTintPeach,
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)
     ) {
         Row(
@@ -380,17 +392,18 @@
             verticalAlignment = Alignment.CenterVertically
         ) {
             Column(modifier = Modifier.weight(1f)) {
-                Text("KIRI ECOSYSTEM", style = MaterialTheme.typography.labelSmall, color = NotionBrandOrangeDeep.copy(alpha = 0.6f), letterSpacing = 1.sp)
-                Text("Hub Dashboard", style = MaterialTheme.typography.titleMedium, color = NotionBrandOrangeDeep, fontWeight = FontWeight.Bold)
-                Text("Marketplace & AI Services.", style = MaterialTheme.typography.bodySmall, color = NotionBrandOrangeDeep.copy(alpha = 0.6f))
+                Text("KIRI ECOSYSTEM", style = MaterialTheme.typography.labelSmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandOrangeDeep.copy(alpha = 0.6f), letterSpacing = 1.sp)
+                Text("Hub Dashboard", style = MaterialTheme.typography.titleMedium, color = if (dark) NotionOnTintDark else NotionBrandOrangeDeep, fontWeight = FontWeight.Bold)
+                Text("Marketplace & AI Services.", style = MaterialTheme.typography.bodySmall, color = if (dark) NotionOnTintDarkMuted else NotionBrandOrangeDeep.copy(alpha = 0.6f))
             }
-            KiriIconBadge(icon = "⚡", backgroundColor = NotionCanvas)
+            KiriIconBadge(icon = "⚡", backgroundColor = if (dark) NotionBrandNavyMid else NotionCanvas)
         }
     }
 }
 
 @Composable
 fun InnovationProgressCard(points: Int) {
+    val dark = isSystemInDarkTheme()
     val rankInfo = remember(points) {
         when {
             points >= 10000 -> Triple("Neural Tier 5 (Apex)", 1.0f, NotionBrandPurple)
@@ -400,6 +413,13 @@
             else -> Triple("Neural Tier 1 (Initiate)", (points / 1000f), NotionPrimary)
         }
     }
+    // Lighten the accent for dark backgrounds so it stays legible instead of
+    // reading as invisible against the dark navy card (see rankInfo.third usage below).
+    val accentOnCard = if (dark) {
+        androidx.compose.ui.graphics.lerp(rankInfo.third, Color.White, 0.35f)
+    } else {
+        rankInfo.third
+    }
 
     Surface(
         modifier = Modifier
@@ -427,7 +447,7 @@
                     modifier = Modifier
                         .size(52.dp)
                         .clip(CircleShape)
-                        .background(rankInfo.third.copy(alpha = 0.1f)),
+                        .background(rankInfo.third.copy(alpha = if (dark) 0.28f else 0.1f)),
                     contentAlignment = Alignment.Center
                 ) {
                     Text("💎", fontSize = 28.sp)
@@ -456,7 +476,7 @@
                             .fillMaxWidth()
                             .height(10.dp)
                             .clip(CircleShape),
-                        color = rankInfo.third,
+                        color = accentOnCard,
                         trackColor = MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.2f),
                         strokeCap = androidx.compose.ui.graphics.StrokeCap.Round
                     )
@@ -467,7 +487,7 @@
                         points.toString(), 
                         style = MaterialTheme.typography.headlineSmall, 
                         fontWeight = FontWeight.Black, 
-                        color = rankInfo.third
+                        color = accentOnCard
                     )
                     Text(
                         "UNITS", 
